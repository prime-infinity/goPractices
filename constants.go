package main

import "fmt"

func main() {

	const myConst int = 34

	fmt.Println(myConst)
}

/*
 remember, constants have to be available at compile time.
 constants can be declared without being used immediatly


 constants can be any of the primitives types

 constants can also be shadowed like normal variables, inner declareations wind
*/
